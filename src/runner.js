import debug from "debug"


// connect to db
// load existingBlocks
// run syncIteration(existingBlocks, options)
// persist new blocks 
// loop

const log = debug('ar-posts-by-tag:main')

export function runner() {

}